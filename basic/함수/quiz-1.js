// 매개변수로 x,y,z 를 받아 곱한 값을 반환하는 multiply 함수를 만들어 보세요. 단, 화살표 함수로 만드세요.

let multiply = (x, y, z) => x * y * z;

/*
위와 동일 - 중괄호 생략 가능
let multiply = (x, y, z) => {
     return x * y * z;
};
*/

console.log(multiply(1, 2, 3));
